// import { creteUser } from "./new/create";

import { creteCategories, creteUserProfile } from "./new/create";

(async function () {
    // console.log(await creteUser());
    // console.log(await creteUserProfile());
    // console.log(await creteCategories());
})()
